Add a Strip tool that dilates a completed polyline into an editable polygon, with width adjustment from the toolbar or `config.ini`.
Its default label and shortcut can also be configured in `config.ini`.
