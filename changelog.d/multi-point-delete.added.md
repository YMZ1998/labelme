Allow Ctrl-clicking multiple polygon vertices and deleting them together with the existing Remove Selected Point action, while preserving the three-point minimum.
Selecting two polygon vertices now deletes the shorter inclusive section of the outline between them.
